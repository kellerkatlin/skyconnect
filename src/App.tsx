export default function App() {
  return (
    <div className="app">
      <div style={{ padding: 40, fontFamily: 'var(--font-display)' }}>
        SkyConnect — port en progreso
      </div>
    </div>
  );
}
